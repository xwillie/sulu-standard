<nav>
    <ul>
    {% for item in navigation_root_tree('main') %}
        <li>
            <a href="{{ content_path(item.url) }}" title="{{ item.title }}">{{ item.title }}</a>
        </li>
    {% endfor %}
    </ul>
</nav>